<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expresspackage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConfirmOrderActivity" >
           <!-- <intent-filter>
                <action android:name="android.intent.action.send"
            </intent-filter>-->
        </activity>
        <activity android:name=".SettingActivity" />
        <activity android:name=".RegulationsActivity" />
        <activity android:name=".WaitToRegisterActivity" />
        <activity android:name=".ShowMapsActivity" />
        <activity android:name=".ShowInformationActivity" />
        <activity android:name=".WaitActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Point1Activity" />
        <activity android:name=".TypeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Point2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>